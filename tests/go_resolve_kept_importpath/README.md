# Resolve kept importpath

Checks that Gazelle can resolve dependencies against a library with a '# keep'
comment on its importpath attribute when the importpath doesn't match what
Gazelle would infer.
